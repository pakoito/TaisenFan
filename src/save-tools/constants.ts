/**
 * Default base filename for emitted .sav / .dsv. Matches the
 * patched ROM that the cartridge popup downloads, so emulators
 * pair the save automatically.
 */
export const PATCHED_ROM_BASENAME = 'Sangokushi_Taisen_Ten_Japan_EN';
